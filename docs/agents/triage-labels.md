# Triage Labels

The skills use five canonical triage roles. This file maps each role to the actual string used in this repo.

- `needs-triage` -> `needs-triage`
- `needs-info` -> `needs-info`
- `ready-for-agent` -> `ready-for-agent`
- `ready-for-human` -> `ready-for-human`
- `wontfix` -> `wontfix`

When a skill mentions a role, apply the mapped string above.
